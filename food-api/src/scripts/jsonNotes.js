// Food- is the table
// ID, Name, type, ethnicity (the keys)- are the columns
// The values/properties of the keys- are the rows

// From 10-19 Joe's Lecture
// A Promise is an object which means it has properties and there are array methods on these properties

// ================The Promise.all way=================
// Allows us to put all of the promises(fetch calls) inside of an array and then use Promise.all() .then((array) => {}) and it will process all of the promises/fetch calls at once.

